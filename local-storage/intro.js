//it allows to store and retrieve data in the browser
//permanent storage//doesnt expire
//data store as a key value pair we can access the data by the key
//we can view local story -only can read it and can delete(but cant edit or change it )
//LS only store strings 
// -JSON.strinngify() - converts object to string
//-JSON.parse() - convert back to object from string

//when to use local storage
//LS use for store insensitive data (because anyone can access the local storage)
//after execution Ls should be cleared


//limitations
//insecure data
//synchronous operations
//limited storage capacity


//main methods
//setItem(key,value)-store k,v pair
//getItem(key)-returns the key in front of key
//key(index)-get the key on a given index -data retrive and specify the data
//length-returns the number of stored item
//removeItem(key)-removes given key with its value
//clear()-deletes everything from the storage
