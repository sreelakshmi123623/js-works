//ask the user for the floor number 1 to 5 keep asking until they give a valid number .
//  if its invalid ,print:"this elevator doesnt go there"

var user = 5;
var count = 1;
if (user<=5 && user>=1) {
   console.log(`${user} th floor`);
   
}

else{
        console.log("this elevator doesnt go there"
);
        
    }
